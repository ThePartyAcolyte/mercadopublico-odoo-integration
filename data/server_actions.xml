<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Acción servidor para importar y analizar manualmente -->
    <record id="action_mercadopublico_sync_and_analyze" model="ir.actions.server">
        <field name="name">Sincronizar y Analizar</field>
        <field name="model_id" ref="model_mercadopublico_api"/>
        <field name="state">code</field>
        <field name="code">
# Run immediate import
model.sync_all()
# Run analysis pipeline
model.process_pending_batch()
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': 'Proceso completado',
        'message': 'Se han importado y analizado las licitaciones recientes.',
        'type': 'success',
        'sticky': False,
        'next': {'type': 'ir.actions.client', 'tag': 'reload'}
    }
}
        </field>
    </record>

    <!-- Acción servidor para sincronizar organismos -->
    <record id="action_sync_buyers" model="ir.actions.server">
        <field name="name">Sincronizar Organismos</field>
        <field name="model_id" ref="model_mercadopublico_api"/>
        <field name="state">code</field>
        <field name="code">
model.sync_buyers()
        </field>
    </record>
</odoo>
